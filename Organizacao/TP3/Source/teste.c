#include <stdio.h>
#include <stdlib.h>

//int main(){

    //int vetor [10];
    /*for(int i = 0; i< 10; i++)
        vetor[i] = (i+1)*2;

    FILE * arq = fopen("teste.dat", "wb");

    fwrite(vetor, sizeof(int), 10, arq);

    fclose(arq);*/

    
    
    
    /*FILE * arq = fopen("teste.dat", "rb");

    fread(vetor, sizeof(int), 10, arq);

    for(int i =0; i< 10; i++)
        printf("%d\n", vetor[i]);

    fclose(arq);*/



    /*FILE * arq = fopen("teste.dat", "ab+");

    fread(vetor, sizeof(int), 5, arq);

    int novoVeotr[] = {999, 999, 999, 999, 999};

    fwrite(novoVeotr, sizeof(int), 5, arq);

    fclose(arq);*/


   /* return 0;
}*/